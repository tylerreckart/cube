import React from 'react';

import Scrambler from './scrambler';

const App = React.createClass ({
  render() {
    return (
      <div>
        <Scrambler/>
      </div>
    );
  }
});

export default App;
